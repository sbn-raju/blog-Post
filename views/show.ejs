<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts</title>
</head>
<body>
    <h1>Posts</h1>
    <% for(let post of postData) {%>
        <h1><%= post.title %></h1>
        <p><%= post.content %></p>
        <p><%= post.image %></p>
    <% } %>
</body>
</html>